# 🍔 Гамбургер-меню «Склад-15» — Руководство

## 📦 Что установлено

Создан полноценный модуль гамбургер-меню с поддержкой:
- ✅ Анимации (framer-motion)
- ✅ Свайпы (react-swipeable)
- ✅ Иконки (lucide-react)
- ✅ Оффлайн-работа
- ✅ Виброотклик
- ✅ Адаптивный дизайн для TSD

---

## 📁 Структура модуля

```
/src/modules/menu/
  ├── HamburgerMenu.tsx      # Основной компонент меню
  ├── MenuItem.tsx           # Элемент меню с поддержкой подменю
  ├── MenuOverlay.tsx        # Затемнение фона
  ├── MenuProvider.tsx       # Context provider
  ├── useMenu.ts            # Хук управления состоянием
  ├── MenuData.ts           # Конфигурация пунктов меню
  └── index.ts              # Экспорт модуля
```

---

## 🎯 Пункты меню

### Основные пункты:
1. **🔄 Синхронизировать** — запускает `useSync()`
2. **🧩 Обновить справочники** — вызывает `updateReferences()`
3. **⚙️ Настройки** — раскрывается подменю:
   - Подключение
   - Пользователь
   - Поведение
   - Синхронизация
4. **👥 Напарник** — переход на `/partner`
5. **🧾 Мои документы** — переход на `/documents`
6. **📊 Статистика и KPI** — модальное окно
7. **🧠 Диагностика** — модальное окно
8. **💬 Обратная связь** — локальный лог
9. **ℹ️ О программе** — модальное окно
10. **🚪 Выйти** — logout handler

---

## 🚀 Как протестировать

### 1. Открыть меню
- Нажать на иконку ☰ в левом верхнем углу (на главной странице)
- Свайпнуть слева направо (работает на touchscreen)

### 2. Закрыть меню
- Нажать на затемненную область
- Свайпнуть меню влево
- Нажать ESC на клавиатуре
- Выбрать любой пункт (меню автоматически закроется)

### 3. Проверить статус онлайн/оффлайн
- В верхней части меню есть индикатор:
  - 🟢 **Онлайн** — зеленый
  - 🔴 **Оффлайн** — красный
- Отключите интернет в DevTools → Network → Offline

### 4. Проверить подменю "Настройки"
- Нажать на "⚙️ Настройки"
- Должно раскрыться 4 подпункта
- Подпункты имеют отступ и левую границу

### 5. Проверить навигацию
- Нажать "👥 Напарник" → должен перейти на `/partner`
- Нажать "🧾 Мои документы" → должен перейти на `/documents`

### 6. Проверить оффлайн-режим
- Переключить в оффлайн (DevTools)
- Пункты с `requiresOnline: true` должны стать серыми
- "🔄 Синхронизировать" и "🧩 Обновить справочники" — недоступны

### 7. Проверить виброотклик (на телефоне/TSD)
- При открытии меню — короткая вибрация (50ms)
- При нажатии на пункт — короткая вибрация (30ms)

---

## 🎨 UI/UX особенности

### Цветовая схема:
- Фон меню: `bg-gray-900/95` с backdrop-blur
- Текст: `text-gray-100`
- Активный пункт: `bg-gray-700/50`
- Иконки: `text-blue-400`
- Отключенные: `opacity-40`

### Анимации:
- Выезд меню слева: `duration-200ms`, `ease-in-out`
- Затемнение фона: `opacity 0 → 1`
- Подменю: `height auto`, `opacity 0 → 1`
- Нажатие: `scale 0.98`

### Размеры:
- Ширина меню: `85vw`, max `400px`
- Высота пункта: min `64px` (для перчаток)
- Иконки: `24px × 24px`
- Touch target: min `64px`

---

## 🧪 Пример тестирования в консоли

```javascript
// Открыть меню программно
const menuBtn = document.querySelector('[aria-label="Открыть меню"]');
menuBtn?.click();

// Проверить статус
console.log('Online:', navigator.onLine);

// Проверить вибрацию
navigator.vibrate(50);
```

---

## 🔧 Расширение меню

### Добавить новый пункт:
Отредактируйте `src/modules/menu/MenuData.ts`:

```typescript
{
  id: 'new-feature',
  label: 'Новая функция',
  icon: Star,
  action: 'navigate',
  actionValue: '/new-feature',
  requiresOnline: false,
}
```

### Добавить подпункты:
```typescript
{
  id: 'parent',
  label: 'Родитель',
  icon: Folder,
  action: 'expand',
  children: [
    {
      id: 'child-1',
      label: 'Ребенок 1',
      icon: File,
      action: 'navigate',
      actionValue: '/child-1',
    }
  ]
}
```

---

## 📝 Известные ограничения

1. **Модальные окна** — заглушки (пока отображается простой popup)
2. **Toast уведомления** — логируются в консоль
3. **Диагностика/Статистика** — требуют реализации отдельных модулей
4. **Обратная связь** — записывается только в консоль

---

## ✅ Чек-лист проверки

- [ ] Меню открывается по клику на ☰
- [ ] Меню открывается свайпом
- [ ] Меню закрывается по клику на overlay
- [ ] Меню закрывается свайпом влево
- [ ] Меню закрывается по ESC
- [ ] Статус онлайн/оффлайн корректен
- [ ] Пункты с requiresOnline серые в оффлайне
- [ ] Подменю "Настройки" раскрывается
- [ ] Навигация работает (Напарник, Документы)
- [ ] Виброотклик работает (если устройство поддерживает)
- [ ] Информация о версии отображается внизу
- [ ] Имя пользователя загружается из IndexedDB
- [ ] Анимации плавные и без задержек

---

## 🚨 Troubleshooting

### Меню не открывается:
1. Проверьте консоль на ошибки
2. Убедитесь, что `MenuProvider` обернут вокруг `BrowserRouter`
3. Проверьте импорты в `App.tsx`

### Свайпы не работают:
1. Убедитесь, что `react-swipeable` установлен
2. Проверьте, что используете touchscreen или эмулируете в DevTools

### Статус оффлайн не меняется:
1. Проверьте вкладку Network в DevTools
2. Используйте реальное отключение сети, а не airplane mode

---

## 📱 Особенности для TSD

- Крупные интерактивные зоны (64px+)
- Виброотклик при взаимодействии
- Работа в перчатках (touch-manipulation)
- Контрастные цвета (WCAG AA)
- Оффлайн-first архитектура
- Быстрые анимации (200ms)

---

## 🎉 Готово к использованию!

Меню полностью интегрировано и готово к работе.  
Откройте http://localhost:3000 и нажмите ☰ в левом верхнем углу!

**Автор**: AI Engineer (Claude Sonnet 4.5)  
**Дата**: 31 октября 2025  
**Версия**: 1.0.0

