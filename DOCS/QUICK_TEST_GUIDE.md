# 🧪 Быстрое руководство по тестированию API интеграции

## 📋 Предварительные требования

### 1. Запустить API сервер
```bash
# API должен быть доступен на:
http://localhost:9000/MobileSMARTS/api/v1
```

### 2. Проверить доступность API
```bash
# Проверка типов документов
curl http://localhost:9000/MobileSMARTS/api/v1/DocTypes

# Проверка metadata
curl http://localhost:9000/MobileSMARTS/api/v1/$metadata
```

---

## 🚀 Запуск приложения

```bash
# В корне проекта
npm run dev

# Приложение откроется на:
http://localhost:3000
```

---

## ✅ Тест-кейсы

### Тест 1: Главный экран (Home.tsx)

**Шаги:**
1. Открыть http://localhost:3000
2. Дождаться загрузки

**Ожидаемый результат:**
- ✅ Отображается индикатор загрузки ("Загрузка типов документов...")
- ✅ Загружаются динамические плитки типов документов из API
- ✅ Каждая плитка показывает:
  - Иконку (автоопределение)
  - Название типа документа
  - Описание
  - Количество документов

**Проверка кэша:**
```javascript
// В консоли браузера
indexedDB.databases().then(console.log)
```

### Тест 2: Список документов (DocumentsByType.tsx)

**Шаги:**
1. На главном экране нажать на любую плитку типа документов
2. Дождаться загрузки

**Ожидаемый результат:**
- ✅ URL изменился на `/docs/{docTypeUni}`
- ✅ Отображается название типа документа
- ✅ Отображается список документов с карточками
- ✅ Каждая карточка содержит:
  - Название документа
  - Описание (если есть)
  - Статус (Новый / В работе / Завершён)
  - Дату создания
  - Дату изменения
  - Пользователя (если есть)
  - Склад (если есть)
  - Штрихкод (если есть)

### Тест 3: Кэширование

**Шаги:**
1. Загрузить главный экран (с интернетом)
2. Открыть DevTools → Application → IndexedDB → WarehouseDB
3. Проверить таблицы `odataDocTypes`, `odataDocuments`, `cacheMetadata`
4. Выключить сервер API
5. Обновить страницу (F5)

**Ожидаемый результат:**
- ✅ Данные загружаются из кэша
- ✅ В консоли: "✅ Loaded DocTypes from cache"
- ✅ Приложение работает без ошибок

### Тест 4: Обработка ошибок

**Шаги:**
1. Выключить API сервер
2. Очистить кэш: DevTools → Application → IndexedDB → Delete WarehouseDB
3. Обновить главный экран

**Ожидаемый результат:**
- ✅ Отображается экран ошибки
- ✅ Текст: "Ошибка загрузки типов документов"
- ✅ Кнопка "Повторить попытку"
- ✅ Нажатие на кнопку повторяет запрос

### Тест 5: Навигация

**Шаги:**
1. Главная → нажать на плитку документа
2. Список документов → нажать кнопку "Назад" в хедере
3. Главная → нажать на "Все документы"

**Ожидаемый результат:**
- ✅ Переход на `/docs/{docTypeUni}`
- ✅ Возврат на `/`
- ✅ Переход на `/documents`

### Тест 6: Обновление данных

**Шаги:**
1. Загрузить главный экран
2. Изменить данные на сервере (добавить/удалить документ)
3. Обновить страницу через кнопку 🔄

**Ожидаемый результат:**
- ✅ Данные обновляются
- ✅ В консоли: "✅ Fetched and cached ... from API"

---

## 🔍 Проверка консоли

### Успешная загрузка
```
✅ Fetched and cached 10 DocTypes from API
✅ Fetched and cached 5 documents from API for PrihodNaSklad
```

### Загрузка из кэша
```
✅ Loaded DocTypes from cache
✅ Loaded 5 documents from cache for PrihodNaSklad
```

### Использование устаревшего кэша
```
⚠️ Failed to fetch DocTypes from API, trying cache: NetworkError
⚠️ Using stale cache for DocTypes
```

---

## 📊 Проверка IndexedDB

### Структура базы данных

```
WarehouseDB (version 4)
├── odataDocTypes (ODataDocumentType[])
│   └── Key: uni
├── odataDocuments (ODataDocument[])
│   └── Key: id
└── cacheMetadata (CacheMetadata[])
    └── Key: key (например, "docTypes", "docs_PrihodNaSklad")
```

### Проверка данных

```javascript
// В консоли браузера
async function checkCache() {
  const db = await window.indexedDB.open('WarehouseDB', 4);
  
  return new Promise((resolve) => {
    db.onsuccess = () => {
      const transaction = db.result.transaction(['odataDocTypes', 'odataDocuments', 'cacheMetadata']);
      const stats = {
        docTypes: 0,
        documents: 0,
        metadata: 0
      };
      
      transaction.objectStore('odataDocTypes').count().onsuccess = (e) => {
        stats.docTypes = e.target.result;
      };
      
      transaction.objectStore('odataDocuments').count().onsuccess = (e) => {
        stats.documents = e.target.result;
      };
      
      transaction.objectStore('cacheMetadata').count().onsuccess = (e) => {
        stats.metadata = e.target.result;
        console.log('Cache Stats:', stats);
        resolve(stats);
      };
    };
  });
}

await checkCache();
```

---

## 🐛 Отладка проблем

### Проблема: Белый экран

**Решение:**
1. Открыть DevTools → Console
2. Проверить ошибки
3. Убедиться что API доступен: `curl http://localhost:9000/MobileSMARTS/api/v1/DocTypes`

### Проблема: "Cannot find module"

**Решение:**
```bash
npm install
npm run dev
```

### Проблема: CORS ошибка

**Решение:**
- API сервер должен разрешать CORS для `http://localhost:3000`
- Добавить заголовки:
  ```
  Access-Control-Allow-Origin: http://localhost:3000
  Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS
  Access-Control-Allow-Headers: Content-Type, Authorization
  ```

### Проблема: Данные не обновляются

**Решение:**
1. Проверить метаданные кэша:
   ```javascript
   // В консоли
   const db = await window.indexedDB.open('WarehouseDB', 4);
   db.result.transaction('cacheMetadata').objectStore('cacheMetadata').getAll().onsuccess = (e) => {
     console.log('Cache Metadata:', e.target.result);
   };
   ```

2. Очистить кэш:
   ```javascript
   // В консоли
   indexedDB.deleteDatabase('WarehouseDB');
   location.reload();
   ```

---

## 📝 Чек-лист перед релизом

- [ ] API сервер доступен и возвращает корректные данные
- [ ] Главный экран загружает типы документов
- [ ] Клик на плитку открывает список документов
- [ ] Список документов отображается корректно
- [ ] Кэширование работает (данные сохраняются в IndexedDB)
- [ ] Оффлайн режим работает (загрузка из кэша)
- [ ] Обработка ошибок работает (экран ошибки, кнопка повтора)
- [ ] Навигация работает (назад, вперед, на главную)
- [ ] Производительность приемлема (< 2 сек первая загрузка)
- [ ] Нет ошибок в консоли
- [ ] Нет ошибок линтера

---

## 🎯 Следующие шаги после успешного тестирования

1. **Детальный просмотр документа**
   - Создать страницу `/docs/:docTypeUni/:docId`
   - Отображение полной информации о документе
   - Отображение строк документа (items)

2. **Редактирование документов**
   - POST/PUT запросы к API
   - Валидация данных
   - Синхронизация изменений

3. **Поиск и фильтрация**
   - Поиск по документам
   - Фильтры по статусу, дате, пользователю
   - Сортировка

4. **Pagination**
   - Постраничная загрузка для больших списков
   - OData `$top`, `$skip`, `$count`

5. **Оптимизация**
   - Lazy loading компонентов
   - Виртуализация списков
   - Сжатие данных в кэше

---

*Подготовлено: Senior Frontend Engineer*  
*Проект: Cleverence Warehouse PWA "Склад-15"*

