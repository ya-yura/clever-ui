# 🔧 Решение проблем - Белый экран

## Проблема: Белый экран при запуске

### Причины и решения:

#### 1. **Vite не запускается**
```bash
# Проверка
npm list vite

# Решение
npm install
npm run dev
```

#### 2. **Порт 3000 занят**
```bash
# Windows - проверка порта
Get-NetTCPConnection -LocalPort 3000

# Освобождение порта
Stop-Process -Name "node" -Force
npm run dev
```

#### 3. **Кэш браузера**
- Откройте DevTools (F12)
- Application → Clear Storage → Clear site data
- Или Ctrl+Shift+R (жесткая перезагрузка)

#### 4. **Ошибки в консоли**
1. Откройте DevTools (F12)
2. Вкладка Console
3. Проверьте ошибки
4. Вкладка Network - проверьте что файлы загружаются

#### 5. **Проблема с зависимостями**
```bash
# Полная переустановка
Remove-Item node_modules -Recurse -Force
Remove-Item package-lock.json -Force
npm install
npm run dev
```

#### 6. **Проверка через npx**
```bash
# Если npm run dev не работает
npx vite
```

### Текущая конфигурация:
- URL: http://localhost:3000
- Vite config: auto-open включен
- Порт: 3000

### Быстрая диагностика:
1. Проверьте терминал - есть ли ошибки?
2. Откройте http://localhost:3000 вручную
3. Проверьте F12 Console на ошибки
4. Перезапустите: Ctrl+C → npm run dev

