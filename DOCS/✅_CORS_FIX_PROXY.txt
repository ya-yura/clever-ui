✅ ИСПРАВЛЕНА ПРОБЛЕМА CORS ЧЕРЕЗ VITE PROXY
============================================

Дата: 31 октября 2025
Время: ~15:30

## Проблема

Все API запросы блокировались CORS-политикой:

```
❌ Access to XMLHttpRequest at 'http://localhost:9000/MobileSMARTS/api/v1/...' 
   from origin 'http://localhost:3000' 
   has been blocked by CORS policy:
   
   Response to preflight request doesn't pass access control check: 
   No 'Access-Control-Allow-Origin' header is present on the requested resource.
```

Это происходит, потому что:
- Клиент работает на `http://localhost:3000` (Vite dev server)
- API сервер на `http://localhost:9000`
- Браузер блокирует cross-origin запросы из соображений безопасности
- API сервер не настроен для разрешения CORS

## Решение: Vite Proxy

Настроен прокси-сервер в Vite, который перенаправляет запросы к API серверу.

### Как работает:

1. **Клиент делает запрос:** `GET /MobileSMARTS/api/v1/Docs`
2. **Vite proxy перехватывает:** "Это для `/MobileSMARTS`"
3. **Прокси перенаправляет:** `http://localhost:9000/MobileSMARTS/api/v1/Docs`
4. **Сервер отвечает прокси**
5. **Прокси возвращает клиенту**

С точки зрения браузера, запрос идёт на тот же origin (localhost:3000), поэтому CORS не блокирует.

### Конфигурация

**Файл:** `vite.config.ts`

```typescript
server: {
  port: 3000,
  open: true,
  // Proxy to bypass CORS in development
  proxy: {
    '/MobileSMARTS': {
      target: 'http://localhost:9000',
      changeOrigin: true,
      secure: false,
      configure: (proxy, _options) => {
        // Логирование всех прокси-запросов
        proxy.on('proxyReq', (proxyReq, req, _res) => {
          console.log('🔄 [PROXY]', req.method, req.url, '→', proxyReq.path);
        });
        proxy.on('proxyRes', (proxyRes, req, _res) => {
          console.log('✅ [PROXY]', proxyRes.statusCode, req.url);
        });
      },
    },
  },
}
```

**Параметры:**
- `target: 'http://localhost:9000'` - куда перенаправлять
- `changeOrigin: true` - изменить Origin header
- `secure: false` - разрешить self-signed сертификаты
- `configure` - добавить логирование

### Изменения в API

**Файл:** `src/services/api.ts`

Теперь по умолчанию используется **относительный путь**:

```typescript
// Было:
const defaultBaseUrl = 'http://localhost:9000/MobileSMARTS/api/v1';

// Стало:
const defaultBaseUrl = '/MobileSMARTS/api/v1';
```

Это позволяет прокси работать:
- **В dev:** прокси перенаправит на `http://localhost:9000`
- **В prod:** нужно настроить через Setup page

## Логирование прокси

В терминале (где запущен `npm run dev`) теперь видны все прокси-запросы:

```
🔄 [PROXY] GET /MobileSMARTS/api/v1/DocTypes → /MobileSMARTS/api/v1/DocTypes
✅ [PROXY] 200 /MobileSMARTS/api/v1/DocTypes

🔄 [PROXY] GET /MobileSMARTS/api/v1/Docs/PrihodNaSklad → /MobileSMARTS/api/v1/Docs/PrihodNaSklad
✅ [PROXY] 200 /MobileSMARTS/api/v1/Docs/PrihodNaSklad
```

В консоли браузера (F12):

```
✅ [API] Config not configured, using relative path (via proxy): /MobileSMARTS/api/v1
🌐 [API] GET /MobileSMARTS/api/v1/DocTypes
✅ [API] Response status: 200
📦 [API] Response data: { value: [...] }
```

## Как применить исправление

### 1. Остановите dev сервер

Нажмите `Ctrl+C` в терминале где запущен `npm run dev`

### 2. Обновите код

```bash
git pull
```

### 3. Запустите dev сервер заново

```bash
npm run dev
```

### 4. Обновите страницу в браузере

`Ctrl+Shift+R` (жесткое обновление)

### 5. Проверьте консоль

Откройте F12 → Console и посмотрите логи. Должны исчезнуть CORS ошибки.

## Для продакшена

В production окружении прокси НЕ работает (это только для dev).

Есть 2 варианта:

### Вариант 1: Настроить CORS на сервере (рекомендуется)

На сервере MobileSMARTS нужно разрешить CORS:

```csharp
// В ASP.NET Core
app.UseCors(policy => 
    policy.AllowAnyOrigin()
          .AllowAnyMethod()
          .AllowAnyHeader());
```

### Вариант 2: Использовать Setup page

В production PWA нужно:
1. Открыть Setup page
2. Ввести полный URL сервера: `http://your-server:9000/MobileSMARTS/api/v1`
3. Убедиться, что сервер доступен с клиента

## Преимущества решения

✅ **Работает сразу** - не нужно настраивать сервер  
✅ **Простое** - один файл конфигурации  
✅ **Логирование** - видны все запросы  
✅ **Для разработки** - не влияет на production  

## Ограничения

⚠️ **Только для dev** - в production нужен CORS на сервере  
⚠️ **Нужен перезапуск** - изменения в vite.config требуют перезапуска dev сервера  

---

**Статус:** ✅ Исправлено
**Файлы изменены:**
- vite.config.ts
- src/services/api.ts

**ВАЖНО:** Перезапустите dev сервер после обновления!

