# 🔍 Устранение проблем с API

## Проблема: "Ошибка загрузки типов документов"

### Шаг 1: Проверьте, что API сервер запущен

Откройте в браузере:
```
http://localhost:9000/MobileSMARTS/api/v1/DocTypes
```

**Ожидаемый результат:** JSON с массивом типов документов
```json
{
  "value": [
    {
      "uni": "PrihodNaSklad",
      "name": "Приход на склад",
      "displayName": "Приход на склад",
      ...
    }
  ]
}
```

**Если видите ошибку:**
- ❌ Сервер не запущен → запустите API сервер
- ❌ 404 Not Found → проверьте URL
- ❌ Connection refused → проверьте порт 9000

### Шаг 2: Откройте консоль браузера (F12)

**Что искать:**

✅ **Успешная загрузка:**
```
✅ API baseURL updated: http://localhost:9000/MobileSMARTS/api/v1
🌐 Fetching DocTypes from API...
✅ Fetched and cached 8 DocTypes from API
```

❌ **CORS ошибка:**
```
Access to fetch at 'http://localhost:9000/...' from origin 'http://localhost:5173'
has been blocked by CORS policy
```

**Решение:** Настройте CORS на сервере:
```
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: GET, POST, PUT, DELETE
Access-Control-Allow-Headers: Content-Type, Authorization
```

❌ **Network Error:**
```
Failed to fetch
net::ERR_CONNECTION_REFUSED
```

**Решение:** Сервер не запущен или не доступен на localhost:9000

### Шаг 3: Проверьте Network tab (F12)

1. Откройте **DevTools (F12)**
2. Вкладка **Network**
3. Обновите страницу
4. Найдите запрос к `/DocTypes`

**Что смотреть:**
- Status: должен быть **200 OK**
- Response: должен содержать JSON с массивом
- Headers: проверьте CORS заголовки

### Шаг 4: Очистите данные

Если видите старые моковые данные:

1. **Через Диагностику:**
   - Меню (☰) → Диагностика
   - "Очистить все данные"

2. **Через консоль (F12):**
   ```javascript
   localStorage.clear();
   indexedDB.deleteDatabase('warehouse-db');
   location.reload();
   ```

### Шаг 5: Проверьте настройки

1. Откройте меню (☰) → Настройки
2. Проверьте "Адрес сервера"
3. Должно быть: `http://localhost:9000/MobileSMARTS/api/v1`

Если неправильно:
1. Диагностика → "Очистить все данные"
2. Пройдите Setup заново

### Шаг 6: Проверьте конфигурацию

В консоли браузера (F12):
```javascript
// Проверить конфигурацию
console.log('Server URL:', localStorage.getItem('app_config'));

// Проверить токен (если требуется)
console.log('Token:', localStorage.getItem('auth_token'));
```

## Частые ошибки

### 1. "No DocTypes available"

**Причина:** API вернул пустой массив

**Проверьте:**
```
GET http://localhost:9000/MobileSMARTS/api/v1/DocTypes
```

Должен вернуть:
```json
{
  "value": [...]  // НЕ пустой массив
}
```

### 2. CORS policy error

**Причина:** Сервер не разрешает запросы с localhost:5173

**Решение на сервере:**
Добавьте CORS заголовки в ответы API

**Временное решение (для разработки):**
1. Используйте расширение браузера для обхода CORS
2. Или добавьте proxy в `vite.config.ts`:
   ```typescript
   export default defineConfig({
     server: {
       proxy: {
         '/MobileSMARTS': {
           target: 'http://localhost:9000',
           changeOrigin: true,
         },
       },
     },
   });
   ```

### 3. "Connection refused"

**Причина:** Сервер не запущен на порту 9000

**Решение:**
1. Запустите API сервер
2. Проверьте, что он слушает на порту 9000
3. Проверьте firewall

### 4. 404 Not Found

**Причина:** Неправильный URL

**Проверьте:**
- Base URL должен быть: `http://localhost:9000/MobileSMARTS/api/v1`
- Endpoint: `/DocTypes` (с заглавной D)
- Полный URL: `http://localhost:9000/MobileSMARTS/api/v1/DocTypes`

## Быстрая проверка

Выполните в консоли браузера (F12):

```javascript
// Проверка 1: Доступен ли API?
fetch('http://localhost:9000/MobileSMARTS/api/v1/DocTypes')
  .then(r => r.json())
  .then(data => console.log('✅ API OK:', data))
  .catch(err => console.error('❌ API Error:', err));

// Проверка 2: Настроен ли baseURL?
console.log('Config:', localStorage.getItem('app_config'));

// Проверка 3: Есть ли кэш?
indexedDB.databases().then(dbs => console.log('Databases:', dbs));
```

## Если ничего не помогло

1. **Полный сброс:**
   ```javascript
   // В консоли браузера
   localStorage.clear();
   sessionStorage.clear();
   indexedDB.deleteDatabase('warehouse-db');
   
   // Перезагрузить
   location.href = '/setup';
   ```

2. **Проверьте сам API сервер:**
   - Логи сервера
   - Документация API
   - $metadata.xml

3. **Создайте issue с информацией:**
   - Скриншот ошибки
   - Console logs (F12)
   - Network tab (F12)
   - Версия браузера
   - Версия API сервера

## Полезные команды

### Проверка через curl:
```bash
curl http://localhost:9000/MobileSMARTS/api/v1/DocTypes
```

### Проверка через PowerShell:
```powershell
Invoke-WebRequest -Uri "http://localhost:9000/MobileSMARTS/api/v1/DocTypes"
```

### Просмотр кэша в DevTools:
1. F12 → Application
2. IndexedDB → warehouse-db
3. Таблицы: odataDocTypes, odataDocuments

---

**Если проблема сохраняется, проверьте документацию:** [API_SERVER_SETUP.md](API_SERVER_SETUP.md)

